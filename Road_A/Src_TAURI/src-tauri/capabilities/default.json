{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the MacFind Road A window.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
