{
  "name": "macfind-roadc-tauri",
  "version": "0.1.0",
  "description": "Road_C · Tauri — macOS hybrid file search (index + searchfs fallback)",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build:frontend": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-shell": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
