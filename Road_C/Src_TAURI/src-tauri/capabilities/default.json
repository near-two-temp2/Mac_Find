{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main window. All heavy lifting (search, Finder reveal, open) goes through custom #[tauri::command] handlers, so only the core window/app permissions are needed here.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:app:default",
    "core:event:default",
    "shell:default"
  ]
}
