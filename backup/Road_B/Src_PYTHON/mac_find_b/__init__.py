"""Road_B (Python) — self-built binary index + bitmask prefilter + fzf scoring.

macOS fast file search, Cling-inspired architecture.

Modules:
    engine  — index building, mmap loading, two-phase search
    gui     — PyQt6 desktop app (search box + result list)
    cli     — index/search command-line entry points for CI smoke tests
"""

__version__ = "0.1.0"
